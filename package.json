{
  "name": "nekoyuki",
  "version": "0.0.1",
  "description": "Discord bot for nekoyuki",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "del /s /q build",
    "build": "tsc --build",
    "start": "npm run clean && npm run build && node build/index.js",
    "startNoClean": "npm run build && node build/index.js"
  },
  "author": "nekover2",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.16.1",
    "glob": "^11.0.0",
    "reflect-metadata": "^0.2.2",
    "sqlite3": "^5.1.7",
    "typeorm": "^0.3.20",
    "typescript": "^5.6.2"
  }
}
